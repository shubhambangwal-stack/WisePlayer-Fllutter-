import 'package:flutter/material.dart';

import '../const/api/onboardingApi.dart';

Widget appLogo([double height = 100, double width = 200]) {
  return SizedBox(height: height, width: width, child: Image.asset(applogo));
}
